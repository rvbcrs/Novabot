{
  "name": "novabot-cloud-export",
  "version": "1.1.4",
  "description": "Download your Novabot account data from the LFI cloud",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "npm run build:wizard && tsx src/index.ts",
    "build:wizard": "cd wizard && npm run build",
    "generate-bundle": "node scripts/generate-wizard-bundle.mjs",
    "build:server": "npm run generate-bundle && tsc && node -e \"const fs=require('fs'),v=require('./package.json').version; const f='dist/server.js'; fs.writeFileSync(f, fs.readFileSync(f,'utf8').replace('__APP_VERSION__',v));\"",
    "build": "npm run build:wizard && npm run build:server",
    "package": "npm run build && node -e \"const v=require('./package.json').version.replace(/\\\\./g,'-'); const {execSync}=require('child_process'); execSync('npx @yao-pkg/pkg dist/index.js --target node20-macos-arm64,node20-macos-x64,node20-windows-x64 --output dist/novabot-cloud-export-v'+v, {stdio:'inherit'});\""
  },
  "dependencies": {
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20",
    "@yao-pkg/pkg": "^5.11.5",
    "tsx": "^4.7.2",
    "typescript": "^5.4.3"
  },
  "pkg": {
    "targets": [
      "node20-macos-arm64",
      "node20-macos-x64",
      "node20-windows-x64"
    ],
    "outputPath": "dist"
  }
}
